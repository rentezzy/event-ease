import { Container, Typography } from "@mui/material";

export const Chat = () => {
  return (
    <div
      style={{
        width: "100%",
        backgroundColor: "#fe7f2d",
        padding:40,
        marginBottom: "20px",
      }}
    >
      <Container>
        <Typography variant="h1">Still in development...</Typography>
      </Container>
    </div>
  );
};
